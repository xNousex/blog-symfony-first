{% extends 'base.html.twig' %}

{% block title %}Modification Article{% endblock %}

{% block body %}
    <h1 class="text-center m-3">Mise à jour d'un article</h1>
    <div class="container border border-light">
        {{ include('article/_form.html.twig', {'button_label': 'Modifier'}) }}

        {{ include('article/_delete_form.html.twig') }}
    </div>
    <div class="row my-2 justify-content-start">
         <a class="btn btn-success w-20" href="{{ path('article_index') }}">Retour à la liste des articles</a>
    </div>
{% endblock %}
